version: 0.2

env:
  #secrets-manager:
    #SONAR_TOKEN: "sonarqube:sonartoken"
    SONAR_TOKEN: "f4f7a4483d733c4d8bc0382931883ca79f7a2544"

phases:
  install:
    commands:
      - apt-get update
      - apt-get install -y openjdk-17-jdk wget jq
      - export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
      - export PATH=$JAVA_HOME/bin:$PATH

  build:
    commands:
      - echo "Running Maven build and SonarQube analysis..."
      - mvn verify sonar:sonar -Dsonar.projectKey=somanaboina-522_devsecops -Dsonar.organization=somanaboina_522 -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=$SONAR_TOKEN
